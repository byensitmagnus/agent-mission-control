# Inventory import requirements

IDs are non-negative integers or digit-only strings with optional surrounding whitespace. Booleans, missing IDs, negative values, and non-digit strings are invalid. Duplicate normalized IDs are invalid.
