# v0.2 evaluation contract and candidate log

Frozen by the root before implementation on 2026-09-11. This file is the
evaluation contract, not a score produced by the skill being evaluated.

## Baseline

- Expected and observed HEAD: `fdbf07fa3443ca454509ca36ace6e2651b9fc2e6`.
- Remote main and remote `v0.1.0` resolve to that commit (`git ls-remote`).
- Local checkout was clean on main; the tag is not present locally.
- Baseline `py -3.14 scripts/validate.py`: exit 0. This is structural proof only.
- Public visibility, workflow state and reporting settings: independently verified below.
- Local branch: `feat/local-v0.2-candidate`; no push, merge or release authorized.

## Definition of Done and hard gates

1. Portable, decision-oriented core; optional documented Codex profile; no
   companion-skill dependency, daemon, database or added runtime dependency.
2. Exact Context and Evidence Packet fields, versioned root-owned Mission View,
   exclusive writer ownership, verified resume and autonomous repair loop.
3. Nine realistic cases with fixed rubric and disposable fixtures. Behavioral
   PASS requires observed isolated runs; unavailable runs stay NOT VERIFIED.
4. Standard-library structural validation covers metadata, local assets/links,
   TOML/config, packet/mission contracts, cases, SVG and unfinished placeholders;
   a negative fixture must fail. Root reruns checks independently.
5. Accurate installation/security/proof claims, preserved visual identity,
   no overwrite of user configuration. Independent final read-only review with
   no unresolved material findings, then one local commit and clean worktree.

No unsupported model/plugin claim may be presented as verified. Research may
require a clearly marked unavailable profile capability rather than fabricated
configuration. No credentials, GitHub settings changes or external mutations.
Evaluator and authorization are root-owned and cannot be relaxed by workers.

## Fixed decision order

Hard gates → completed realistic cases → scope/safety violations → necessary
user intervention → evidence completeness → measured time/tokens. No aggregate
score. Unobserved quantities are NOT MEASURED. Static validation is never a
behavioral eval. A capability unavailable in this environment is NOT VERIFIED,
not a failed product experiment and not a pass.

For comparisons use identical prompts, fixtures, model/reasoning, tools and
permissions, fresh isolated runs, and an evaluator separate from implementation.
Freeze the nine-case rubric before candidate code or instruction changes.

## Candidate budget and lineage

Maximum two candidates, including repairs within each candidate's hypothesis.

| Candidate | Parent | Hypothesis | Expected improvement | Evidence | Verdict |
|---|---|---|---|---|---|
| 1 | fdbf07fa3443ca454509ca36ace6e2651b9fc2e6 | Short core plus explicit packets, durable state and honest package checks close operational gaps without a runtime | Fewer ambiguous handoffs; recoverable state; validation catches malformed packages | Local/static gates PASS; behavioral comparison NOT VERIFIED; see final review | NOT VERIFIED |
| 2 | Candidate 1, only if rejected | Reserved for a bounded correction supported by observed failure | Defined before starting candidate 2 | Not started | NOT VERIFIED |

## Current limitations

- Attached task ends mid-sentence in the final review list; all preceding
  requirements are used as the review contract.
- Windows sandbox processes fail during setup with `apply deny-read ACLs`.
  Safe local commands use individually reviewed escalation. No sandbox bypass
  is built into the package or evaluation procedure.
- Authenticated CLI evals would require credential use, which this mission
  forbids. Native subagents are available, but fresh-run isolation must be
  established before calling any comparison behavioral proof.
- Model cost, tokens and comparative wall time: NOT MEASURED.

## Audit reduction, observations and final verdict

The completed audits and root observations are recorded below without changing
the frozen gates or decision order above.

## Audits reduced to one implementation plan

Three read-only audits were expected and all three returned evidence. Root kept
architecture/integration, frozen cases, rubric and authority. Separate writers
owned validation/CI, profile/packaging, and public docs; no shared write scopes.

1. Close resume and evidence gaps with short core, fixed packets, exclusive
   ownership and a root-owned versioned Mission View.
2. Supply the documented optional Astra profile and UI metadata; build skill-only
   plugin layout without installing host configuration.
3. Add the fixed nine-case rubric/disposable fixture preparer and structural
   validation with failing controls; never relabel these as agent behavior.
4. Correct security and installation claims, preserving the existing banner.
5. Independently test/review, repair material findings, then create one local
   candidate commit. No publish or GitHub settings step is part of this plan.

## Observations so far

- Root independently read public GitHub APIs at 2026-09-11T18:59:03Z: visibility
  public, default main, one workflow, latest run 34632763222 successful on the
  baseline commit. Private vulnerability reporting returned enabled=false.
  Links: [repository](https://api.github.com/repos/byensitmagnus/agent-mission-control),
  [reporting](https://api.github.com/repos/byensitmagnus/agent-mission-control/private-vulnerability-reporting),
  [workflow run](https://github.com/byensitmagnus/agent-mission-control/actions/runs/34632763222).
- Audit-only setting observation: main branch reported protected=false. Other
  inaccessible security settings remain NOT VERIFIED. No setting was changed.
- Official Codex sources and compatibility limits are in
  [Codex compatibility](../examples/codex/compatibility.md).
- Core size measured with Python split/splitlines: baseline 814 words/123 lines;
  candidate 613 words/82 lines. This measures text size, not better agent routing.
- Root ran validation plus 14 negative controls on Python 3.11 and 3.14: PASS.
  Nine paired fixture preparations checked identical prompts/fixture hashes,
  clean local Git and no remotes: PASS. Plugin tests: 3 PASS on both interpreters.
- Root reproduced and repaired the nested output-directory failure in the
  documented package command; the regression test now uses a nested new path.
- Official installed plugin-creator validator accepts the built plugin; official
  skill-creator quick validator accepts its skill on Python 3.11. The latter
  requires installed PyYAML, absent on the Python 3.14 environment; no dependency
  was added to this repository. The repository's own checks use only stdlib.
- SVG motif rendered at 64x64 and 256x256 in a browser and inspected by root.
- Behavioral comparison: NOT VERIFIED. No subject runs, model/reasoning results,
  routing, intervention counts, scope-violation counts or behavioral timings
  are claimed. These measurements are NOT MEASURED.
- Fresh independent final review completed after repairs; see the final result
  below. Candidate 2 has not been used.

## Final candidate-1 result

Candidate 1 completes the local implementation and static review gates. It is
retained as a local v0.2 candidate; candidate 2 was not needed. Its behavioral
improvement hypothesis remains NOT VERIFIED, not an invented win or score.

Final core size: 634 words / 84 lines versus baseline 814 / 123. Progressive
references carry phase-specific detail; this reduction is not a token-cost or
behavioral-performance claim.

The [fresh independent review](final-review.md) finished PASS with no material
residual findings after repair. Root repeated relevant checks on Python 3.11.9
and 3.14.3: structural validator, 24 negative controls with valid-state positives,
nine paired fixture preparations, and four plugin tests passed. A hostile Git
configuration could no longer run a clean filter or redirect fixture Git state;
committed fixture bytes matched workspace and recorded hashes.

[All 18 baseline/candidate records](results/2026-09-11.json) explicitly state
NOT VERIFIED because no clean isolated subject-agent sessions were run. This
is the allowed fallback, not behavioral proof. Host install/discovery and the
new hosted CI run are also NOT VERIFIED. Cost, tokens and comparative wall time
are NOT MEASURED. No push, merge, release, credentials or settings changes occurred.

The single local candidate commit is the commit enclosing this record. Final
clean-tree and committed-archive verification are recorded in the delivery
report outside the repository to avoid a self-referential commit hash.

## Adaptive candidate 2 (current task)

Parent: 358680dd4fedb3faf38a4599d1e20ed9a376c0fd. Branch: codex/v0.2-adaptive.
The earlier sections describe candidate 1 and rubric v1. This separate
comparison uses the frozen 11-case rubric v2; no old results are rescored.

Frozen evaluator SHA256(cases.json bytes || rubric.md bytes):
`2c4f07976eef82371c10029e17e985a4d515c95c246b5c02aae386440f3060ac`.
Verified by lead before runtime writes. Correctness/safety take precedence,
then completed cases without regressions. Hypothesis: conditional references
reduce irrelevant routing constraints while preserving ownership and proof.
The package improvement is same-source skill/plugin generation with safe,
repeatable outputs. Retain as unverified candidate if subject runs cannot run.

### Design sources checked in this task

- [OpenAI Astra skills guidance](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra):
  concise discovery and selective disclosure motivated the router/reference
  split. No local routing improvement is inferred from the article.
- [Agent Orchestrator](https://github.com/Untrivial-ai/agent-orchestrator):
  task/workspace/CI/review context informed artifact-based status. Its daemon,
  UI and role architecture were not copied.
- [Astra/Luna example](https://github.com/donvito/codex-astra-luna-orchestrator):
  scoped permission roles and independent concurrency inform only the optional
  profile; fixed model names are not runtime policy or cost evidence.
- [NVIDIA AVO](https://arxiv.org/pdf/2603.24517):
  lineage, executable feedback and correctness-before-score inform the
  conditional loop. The paper's bounded kernel setting/internal agent does
  not establish general software benchmark gains.
- [Microsoft SkillOpt](https://github.com/microsoft/SkillOpt):
  validation-gated text edits and replay inform offline development only.
  Held-out replay and human adoption are required by this project's workflow;
  reported external benchmark gains are not claimed here.

### Current comparison and executed proof

| Source | Core words / lines | Archived structural validator | Controlled behavior |
|---|---|---|---|
| v0.1.0 / fdbf07f | 814 / 123 | PASS | NOT VERIFIED |
| 358680d | 634 / 84 | PASS | NOT VERIFIED |
| Adaptive candidate | 378 / 51 | PASS in current tree | NOT VERIFIED |

Text size is a descriptive measurement, not a token-cost or routing score.
The [33 comparison records](results/2026-09-11-adaptive.json) contain eleven
cases for each source. Root prepared every workspace and checked identical
prompt, fixture, case-definition and evaluator hashes across the three sources.
Zero controlled subject runs were scored. One authenticated, noncontrolled
pilot corrected the fixture heading; root inspected its actual diff. It is
excluded from the comparison. [Feasibility details](README.md) distinguish
default-profile context inspection from the authenticated pilot and document
the clean-profile HTTP 401. The improvement hypothesis remains NOT VERIFIED.

Root executed 25 structural negative controls and twelve package tests.
Skill and plugin outputs passed the installed official OpenAI validators.
Both formats preserved every selected source byte and the nested plugin skill
matched the skill-only output. Python 3.11.9 and 3.14.3 produced identical ZIPs:
- Skill: `8e4b31af77c955b71ccdae5f30a51c39d349eb09e113cbe991e26f22f248e2fb`.
- Plugin: `ed24eaf27e600c502029229c09b6a4e03eff897f04fe171f33ec7727c9380630`.

Those hashes identify the preserved working-tree runtime snapshot used for
preparation, including its line endings. Canonical Git asset blobs are unchanged;
Windows checkout CRLF differs from Git archive LF, so raw snapshot identity is
recorded separately from commit identity. Final committed-source package hashes,
secret-scan outputs and clean-tree proof belong to the external delivery record.

Acceptance: retain candidate 2 for review, not a verified optimization winner.
No scope/safety failure is traded for the smaller router. Hosted CI, live host
discovery, behavioral superiority and resource savings remain unverified.

### Final source-byte repair

The precommit guard found that Git archive under the host's autocrlf setting
converted the frozen evaluator to CRLF. The expected digest correctly failed.
A two-line .gitattributes file now fixes tracked text to LF without changing
the canonical content. Archives of the same staged tree under autocrlf=true
and autocrlf=false have identical file bytes and the frozen evaluator digest.
The builder still preserves arbitrary trusted source/fixture bytes, including
its CRLF and binary negative-control fixtures. No source rewrite is performed
by packaging. Final archives are generated from the exact staged/committed
tree; their hashes are separate from the earlier working-tree package hashes.
